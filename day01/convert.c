#include <stdio.h>

int main(){
    int m = 3;
    int n = 2;
    double a = (double)m/n;
    printf("m/n:%lf\n",a);
    // scanf("%lf", &r);

    // double len = 2* pi * r;
    // double area = r * r * pi;

    // printf("circle's length:%.2lf, circle's area:%.2lf", len, area);
    return 0;
}